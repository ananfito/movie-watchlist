import apiKey from "./config.js"

// function to render data stored in local storage
// similar renderResults() in index.js 
// only renders if local storage contains data

// function to remove from watchlist
// clears specified movie from array stored in local storage
// probably use .splice(index, numberOfItemsTORemove)